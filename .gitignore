# Build artifacts
target/

# Python
__pycache__/
*.abi3.so
*.so
*.pyd

# Build lock
.pbuild.lock

# mkdocs build output
site/

# Linux build output
software/linux/
software/bin
software/build

# All test build artifacts (cloned sources, built spike, generated/compiled
# test ELFs, riscof work dir, JSON result files) live under testing/builds/.
testing/builds/
# Legacy: pre-2026-04 paths, kept until safe to remove.
testing/riscof/riscof_work/
testing/vector/third_party/
testing/vector/build/
testing/vector/results/
